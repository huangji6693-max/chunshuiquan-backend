spring.application.name=backend

# Agora RTC — set AGORA_APP_ID and AGORA_APP_CERTIFICATE as env vars (Railway / local .env)
agora.app-id=${AGORA_APP_ID}
agora.app-certificate=${AGORA_APP_CERTIFICATE}
